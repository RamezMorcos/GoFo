/**
 * Just a class which contain the hour of the booking time and a boolean to check if its free or reserved
 */
public class Reserved_hours {
    int hour;
    boolean free_to_reserve;
    Reserved_hours(){
        free_to_reserve=true;
    }
}
